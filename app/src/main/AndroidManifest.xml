<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="co.wishroll">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".WishRollApplication"
        android:allowBackup="true"
        android:debuggable="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:grantUriPermissions="true"
            android:exported="false"
            tools:replace="android:authorities">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity android:name=".views.upload.TaggingActivity"/>

        <activity
            android:name=".views.reusables.VideoActivity"
            android:theme="@style/Fullscreen"/>

        <activity android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat"/>

        <activity android:name=".views.upload.UploadActivity" />
        <activity android:name=".views.reusables.UserList" />
        <activity android:name=".views.reusables.ImageActivity" />
        <activity android:name=".views.profile.ContactActivity" />
        <activity
            android:name=".views.home.SearchActivity"
            android:windowSoftInputMode="stateVisible" />
        <activity android:name=".views.profile.EditProfileActivity" />
        <activity android:name=".views.profile.ProfileViewActivity" />
        <activity
            android:name=".views.home.MainActivity"
            android:launchMode="singleTask" />
        <activity android:name=".views.registration.AgeSignupActivity" />
        <activity android:name=".views.registration.UsernameSignupActivity" />
        <activity android:name=".views.registration.PasswordSignupActivity" />
        <activity android:name=".views.registration.SignupActivity" />
        <activity
            android:name=".views.registration.LoginActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".views.reusables.SplashScreen"
            android:theme="@style/splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.registration.WelcomeActivity"
            android:theme="@style/AppTheme" />
    </application>

</manifest>